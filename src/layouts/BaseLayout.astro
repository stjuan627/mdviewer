---
import 'katex/dist/katex.min.css';
import '@/styles/global.css';
import { ClientRouter } from 'astro:transitions';
import { WorkbenchSidebar } from '@/components/WorkbenchSidebar';
import type { Locale } from '@/lib/i18n';
import { localeToLangTag } from '@/lib/i18n';

interface Props {
  title: string;
  description?: string;
  canonicalUrl?: string;
  schema?: Array<Record<string, unknown>>;
  appShell?: boolean;
  showSidebar?: boolean;
  locale?: Locale;
  alternateLocales?: Array<{ locale: Locale | 'x-default'; href: string }>;
}

const {
  title,
  description = 'Free online markdown viewer with live preview. Render GFM, LaTeX, and Mermaid instantly.',
  canonicalUrl,
  schema = [],
  appShell = true,
  showSidebar = true,
  locale = 'en',
  alternateLocales = [],
} = Astro.props;
---

<!doctype html>
<html lang={localeToLangTag[locale]} data-locale={locale} data-lang-tag={localeToLangTag[locale]}>
  <head>
    <script is:inline>
      (function() {
        const root = document.documentElement;

        function resolveTheme() {
          return localStorage.getItem('mdviewer.theme') || (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
        }

        function applyTheme() {
          root.classList.toggle('dark', resolveTheme() === 'dark');
        }

        function applyLocale() {
          const locale = root.dataset.locale;
          const langTag = root.dataset.langTag;

          if (!locale) {
            return;
          }

          root.lang = langTag || locale;
          root.dataset.locale = locale;
        }

        root.classList.add('is-loading');
        applyTheme();
        applyLocale();
        
        window.addEventListener('load', () => {
          setTimeout(() => {
            root.classList.remove('is-loading');
          }, 10);
        });

        document.addEventListener('astro:after-swap', () => {
          applyTheme();
          applyLocale();
        });
        document.addEventListener('astro:page-load', () => {
          applyTheme();
          applyLocale();
        });
      })();
    </script>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="192x192" href="/android-chrome-192x192.png" />
    <link rel="icon" type="image/png" sizes="512x512" href="/android-chrome-512x512.png" />
    <link rel="icon" href="/logo-square.svg" type="image/svg+xml" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="mask-icon" href="/logo-square.svg" color="#2C2421" />
    <meta name="msapplication-TileColor" content="#2C2421" />
    <meta name="msapplication-TileImage" content="/mstile-150x150.png" />
    <meta name="msapplication-config" content="/browserconfig.xml" />
    <meta name="theme-color" content="#2C2421" />
    <link rel="manifest" href="/site.webmanifest" />
    <ClientRouter />
    <title>{title}</title>
    <meta name="description" content={description} />
    {canonicalUrl ? <link rel="canonical" href={canonicalUrl} /> : null}
    {alternateLocales.map((entry) => (
      <link rel="alternate" hreflang={entry.locale === 'x-default' ? entry.locale : localeToLangTag[entry.locale]} href={entry.href} />
    ))}
    <script defer data-domain="mdviewer.net" src="https://app.pageview.app/js/script.js"></script>
    {schema.map((entry) => (
      <script is:inline type="application/ld+json" set:html={JSON.stringify(entry)} />
    ))}
  </head>
  <body class:list={[appShell && 'app-shell-body']}>
    {appShell ? (
      <div class="app-container">
        {showSidebar ? <WorkbenchSidebar client:load locale={locale} /> : null}
        <main class="main-content">
          <slot />
        </main>
      </div>
    ) : (
      <slot />
    )}
  </body>
</html>
