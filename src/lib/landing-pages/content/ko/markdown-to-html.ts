export const markdownToHtmlInitialMarkdownKo = `## 마크다운 HTML 변환기 - 브라우저에서 .md를 깨끗한 HTML로 변환

이 도구는 브라우저에서 바로 사용할 수 있는 무료 **마크다운 HTML 변환기**입니다. 원본 \`.md\` 문서를 붙여 넣거나 파일로 업로드하면 렌더링 결과를 확인한 뒤, CMS, 이메일 빌더, 위키, 웹앱에 바로 넣을 수 있는 의미론적 HTML을 복사할 수 있습니다. 설치, 회원가입, LaTeX 도구 체인, Pandoc 설정 없이 변환을 완료할 수 있습니다.

*"마크다운 HTML 변환"*, *"마크다운 HTML 변환기"*, *"Markdown to HTML"*, *"MD HTML 변환"*, *"README HTML 변환"* 같은 검색어로 찾고 있었다면 이 페이지는 바로 그 작업을 위해 만들어졌습니다. Markdown을 입력하고 화면에서 확인한 뒤, 깨끗한 HTML만 가져가면 됩니다.

## 왜 Markdown을 HTML로 변환하나요?

Markdown은 쓰기 편한 작성 형식이고, HTML은 실제 게시 환경에서 필요한 형식입니다. 블로그, CMS, 뉴스레터, 지식 베이스, 트랜잭션 메일, 자체 웹앱은 대부분 최종적으로 HTML 구조를 필요로 합니다. 마크다운 HTML 변환은 문서를 직접 수정하지 않고도 이 간극을 메워 줍니다.

사람들이 마크다운 HTML 변환기를 찾는 대표적인 이유는 다음과 같습니다.

- **CMS에 게시** — Webflow, Ghost, WordPress, Sanity 같은 CMS에 표와 목록이 흐트러지지 않는 HTML을 붙여 넣습니다.
- **Notion, Confluence, 사내 위키에 정리** — 많은 리치 텍스트 편집기는 원시 Markdown보다 붙여 넣은 HTML을 더 안정적으로 구조화합니다.
- **뉴스레터와 이메일 블록 제작** — Mailchimp, Substack, Beehiiv, 트랜잭션 메일 템플릿에 넣을 의미론적 HTML을 만듭니다.
- **README와 기술 문서 전달** — GitHub README를 도움말 센터, 파트너 포털, 온보딩 페이지에 바로 넣을 수 있는 마크업으로 바꿉니다.
- **웹앱에 렌더링 결과 삽입** — Markdown 파서가 아니라 이미 렌더링된 HTML 문자열을 기대하는 컴포넌트에 넣습니다.
- **콘텐츠 마이그레이션** — 오래된 Markdown 아카이브를 \`.md\` 파일 대신 HTML 붙여넣기를 받는 시스템으로 옮깁니다.

## 마크다운 HTML 변환기는 어떻게 작동하나요?

사용 흐름은 세 단계로 단순하게 설계했습니다.

1. **Markdown 붙여 넣기 또는 업로드** — 원본 \`.md\` 문서를 붙여 넣거나 Markdown 파일을 업로드합니다.
2. **렌더링된 HTML 미리보기 확인** — 제목, 목록, 표, 링크, 코드 블록이 목적지에서 기대하는 구조로 보이는지 확인합니다.
3. **HTML 복사 또는 다운로드** — HTML 조각을 복사하거나 바로 열 수 있는 \`.html\` 파일로 내려받습니다.

| 단계 | 작업 | 결과 |
| --- | --- | --- |
| 1 | Markdown 붙여 넣기 또는 업로드 | 즉시 렌더링되는 미리보기 |
| 2 | 렌더링 구조 확인 | 목적지에 맞는 HTML인지 확인 |
| 3 | HTML 복사 또는 \`.html\` 다운로드 | 재사용 가능한 브라우저용 마크업 |

## 마크다운 HTML 변환기에서 "깨끗한 HTML"이 뜻하는 것

모든 마크다운 HTML 변환기가 실제 게시에 적합한 HTML을 내놓는 것은 아닙니다. 어떤 도구는 프레임워크 전용 래퍼를 덧붙이고, 어떤 도구는 추적용 속성을 넣거나 의미 있는 태그를 스타일링용 \`<span>\`으로 바꿔 버립니다. 이 변환기는 그 반대를 지향합니다.

- **의미론적 태그** — 제목은 \`<h1>\`부터 \`<h6>\`까지, 목록은 \`<ul>\` / \`<ol>\` / \`<li>\`, 표는 \`<table>\` / \`<thead>\` / \`<tbody>\`, 코드는 \`<pre><code>\`로 렌더링됩니다.
- **프레임워크 종속 없음** — Tailwind 클래스, React 하이드레이션 마커, 빌드 시점 해시가 없어 어떤 CMS나 앱에도 붙여 넣기 쉽습니다.
- **예측 가능한 속성** — 링크는 \`href\`, 이미지는 \`src\`와 \`alt\`, 코드 펜스는 언어 힌트를 클래스 형태로 유지합니다.
- **스크립트나 추적 코드 없음** — 복사하는 HTML은 미리보기에서 확인한 구조 그대로입니다.

\`\`\`html
<h2>섹션 제목</h2>
<p><strong>강조</strong>와 <a href="/docs">링크</a>가 있는 문단입니다.</p>
<pre><code class="language-ts">const html = renderMarkdown(markdown);</code></pre>
\`\`\`

## 변환 후에도 유지되는 GitHub Flavored Markdown 기능

- **제목**은 의미론적 제목 태그 계층을 유지합니다.
- **표**는 올바른 \`<table>\` 마크업과 열 정렬을 유지합니다.
- **작업 목록**은 체크박스 형태의 리스트 항목으로 변환됩니다.
- **펜스 코드 블록**은 구문 강조에 필요한 언어 클래스 힌트를 유지합니다.
- **각주**, **취소선**, **자동 링크**가 깨끗하게 보존됩니다.
- **인라인 코드**, **굵게**, **기울임**은 \`<code>\`, \`<strong>\`, \`<em>\`으로 남습니다.

Markdown에 LaTeX 수식이나 Mermaid 다이어그램이 포함된 경우 미리보기에서 시각적으로 확인할 수 있습니다. 다만 CMS나 이메일에 붙여 넣을 때는 위의 GFM 기능을 중심으로 문서를 작성했을 때 가장 안정적인 결과를 얻을 수 있습니다.

## 마크다운 HTML 변환기와 다른 방식 비교

| 도구 | 설치 필요 | 실시간 미리보기 | 복사 가능한 HTML 조각 | 비용 |
| --- | --- | --- | --- | --- |
| **이 변환기** | 없음 | 있음 | 있음 | 무료 |
| Pandoc | 필요 (CLI) | 없음 | 있음 | 무료 |
| 정적 사이트 생성기 (Astro, Hugo, Eleventy) | 필요 | 일부 | 즉시 복사 어려움 | 다양 |
| Typora | 필요 (데스크톱 앱) | 있음 | 일부 | 유료 |
| CMS에 원시 Markdown 붙여넣기 | 없음 | 일부 | 없음, 서식이 깨질 수 있음 | 다양 |

"지금 이 Markdown을 HTML로 바꿔야 한다"는 일회성 작업이라면 브라우저 기반 마크다운 HTML 변환기가 CLI나 빌드 환경을 갖추는 것보다 훨씬 가볍습니다.

## HTML 복사 전 최종 체크리스트

- [x] Markdown을 붙여 넣거나 파일 업로드
- [ ] 미리보기에서 제목, 목록, 표, 코드 블록이 의도대로 보이는지 확인
- [ ] 붙여넣기용 **Copy HTML** 클릭, 또는 \`.html\` 파일 다운로드
- [ ] CMS, 이메일 도구, 위키, 웹앱에 HTML 삽입
- [ ] 필요한 경우 자체 CSS로 스타일 조정

위 샘플을 지우고 원하는 Markdown을 입력하면 바로 마크다운 HTML 변환을 시작할 수 있습니다.
`;
